{
  "name": "CS6035 Machine Learning Project",
  "image": "continuumio/miniconda3",
  "features": {
    "ghcr.io/devcontainers/features/common-utils:2": {
      "installZsh": "false",
      "installOhMyZsh": "false",
      "upgradePackages": "true"
    }
  },
  "postCreateCommand": "conda config --set remote_read_timeout_secs 180.0 && conda env create -f 2_ML/env.yml && sudo apt-get update && sudo apt-get install -y zip git-lfs && git lfs install && git config --global --add safe.directory /workspaces/cs6035",
  "postAttachCommand": "conda init bash",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.debugpy"
      ],
      "settings": {
        "python.defaultInterpreterPath": "/opt/conda/envs/cs6035_ML/bin/python"
      }
    }
  },
  "remoteEnv": {
    "PATH": "${containerEnv:PATH}:/opt/conda/bin:/opt/conda/condabin"
  }
}